# إعداد قاعدة البيانات - Supermarket System

## خطوات إعداد قاعدة البيانات

### 1. الوصول إلى Supabase Dashboard
1. اذهب إلى [Supabase Dashboard](https://supabase.com/dashboard)
2. اختر مشروعك: `ymdqcflcnczsjdadhulj`
3. اذهب إلى **SQL Editor**

### 2. تشغيل ملف إنشاء الجداول
1. انسخ محتوى ملف `database/create_tables.sql`
2. الصقه في SQL Editor
3. اضغط **Run** لتنفيذ الأوامر

### 3. التحقق من إنشاء الجداول
اذهب إلى **Table Editor** وتأكد من وجود الجداول التالية:
- `products` - جدول المنتجات
- `invoices` - جدول الفواتير  
- `invoice_items` - جدول عناصر الفاتورة

### 4. إعداد Row Level Security (RLS)
تم تفعيل RLS تلقائياً مع الأوامر، مما يعني:
- المستخدمون يمكنهم رؤية منتجاتهم فقط
- الفواتير محمية بحيث يرى كل مستخدم فواتيره فقط
- عناصر الفاتورة محمية بنفس الطريقة

### 5. البيانات التجريبية
تم إدراج 5 منتجات تجريبية:
- كوكا كولا - 2.50 ريال
- شيبس - 1.50 ريال  
- خبز - 0.75 ريال
- حليب - 3.00 ريال
- جبن - 5.50 ريال

## هيكل قاعدة البيانات

### جدول المنتجات (products)
```sql
- id: UUID (Primary Key)
- name: VARCHAR(255) - اسم المنتج
- price: DECIMAL(10,2) - السعر
- barcode: VARCHAR(100) - الباركود (فريد)
- description: TEXT - الوصف
- category: VARCHAR(100) - الفئة
- stock: INTEGER - المخزون
- created_at: TIMESTAMP - تاريخ الإنشاء
- updated_at: TIMESTAMP - تاريخ التحديث
```

### جدول الفواتير (invoices)
```sql
- id: UUID (Primary Key)
- user_id: UUID - معرف المستخدم
- total: DECIMAL(10,2) - المجموع الكلي
- created_at: TIMESTAMP - تاريخ الإنشاء
- updated_at: TIMESTAMP - تاريخ التحديث
```

### جدول عناصر الفاتورة (invoice_items)
```sql
- id: UUID (Primary Key)
- invoice_id: UUID - معرف الفاتورة
- product_id: UUID - معرف المنتج
- quantity: INTEGER - الكمية
- subtotal: DECIMAL(10,2) - المجموع الفرعي
- created_at: TIMESTAMP - تاريخ الإنشاء
```

## الميزات التلقائية

### 1. حساب المجموع الفرعي
- يتم حساب `subtotal` تلقائياً عند إضافة أو تعديل عنصر في الفاتورة
- الصيغة: `quantity × product_price`

### 2. تحديث مجموع الفاتورة
- يتم تحديث `total` في الفاتورة تلقائياً عند:
  - إضافة عنصر جديد
  - تعديل كمية عنصر موجود
  - حذف عنصر من الفاتورة

### 3. تحديث التواريخ
- يتم تحديث `updated_at` تلقائياً عند تعديل أي سجل

## اختبار التطبيق

### 1. مسح الباركود
- استخدم الباركودات التالية للاختبار:
  - `1234567890123` - كوكا كولا
  - `1234567890124` - شيبس
  - `1234567890125` - خبز
  - `1234567890126` - حليب
  - `1234567890127` - جبن

### 2. إضافة منتج جديد
- امسح باركود غير موجود
- سيظهر لك نموذج لإضافة منتج جديد

### 3. إدارة الفواتير
- يمكن إنشاء فواتير جديدة
- عرض الفواتير السابقة
- طباعة الفواتير

## استكشاف الأخطاء

### مشاكل شائعة:
1. **خطأ في الاتصال**: تأكد من صحة URL و API Key
2. **خطأ في الصلاحيات**: تأكد من تسجيل الدخول
3. **خطأ في البيانات**: تأكد من تشغيل ملف SQL

### سجلات الأخطاء:
- تحقق من **Logs** في Supabase Dashboard
- استخدم **Network** tab في المتصفح لمراقبة الطلبات

## الأمان

- جميع الجداول محمية بـ Row Level Security
- المستخدمون يمكنهم الوصول لبياناتهم فقط
- الباركودات فريدة لمنع التكرار
- التحقق من صحة البيانات على مستوى قاعدة البيانات
